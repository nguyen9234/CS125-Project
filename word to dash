char dash(char a)
{
    srand(time(NULL));
    char* word;
    *word = a;
    printf("%s", word);
    
    int len, x;
    len = strlen(word)-1;
    for(x=0; x<len; x++)
    {
        printf("_ ");
    }
    
}
